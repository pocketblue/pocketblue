[Unit]
# Extend timeout for fastrpc-adsp device
# The ADSP may take longer to initialize on QCM6490 due to:
# - LPASS audio clock controller deferred probe
# - Remote processor initialization sequence
# Default 90s timeout may not be sufficient
JobTimeoutSec=180
JobRunningTimeoutSec=180
